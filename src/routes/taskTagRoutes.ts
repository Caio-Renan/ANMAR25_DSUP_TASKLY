import { Router } from 'express';

const router = Router();

router.post('/tasks/:taskId/task-tags', async (req, res) => {
});

router.delete('/tasks/:taskId/task-tags', async (req, res) => {});

export default router;
